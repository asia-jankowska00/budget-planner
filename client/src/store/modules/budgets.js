// initial state
const state = () => ({
    budgets: []
})

// getters
const getters = {
    budgets: state => state.budgets
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
    state,
    getters,
    actions,
    mutations
}